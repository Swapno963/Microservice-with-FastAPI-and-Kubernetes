apiVersion: v1
kind: Service
metadata:
  name: product-service-lb
  labels:
    app: product-service
spec:
  type: LoadBalancer
  selector:
    app: product-service
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http

